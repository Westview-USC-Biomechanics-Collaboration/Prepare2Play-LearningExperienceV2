[project]
name = "prepare2play-learningexperiencev2"
version = "0.1.0"
description = "Second version of student created vector overlay processing code. Currently in development."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
]
